# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.7] - 2026-09-01

### 修复

- **同一集跨天发布会被推两次。** 同集归并只在**单次轮询批次内**生效，而一个字幕组从
  Baha / ABEMA / CR / B-Global 各压一版时，四版并不是同时发的 —— 实测同一部番同一集，
  CR 比 Baha 早一天、ABEMA 比 Baha 晚五天都出现过。于是先到的当轮推走，第二天后到的
  被当成全新条目再推一次。现在新增跨轮次时间窗（配置项 `rss_episode_dedup_window_hours`，
  默认 48 小时）：某一集推过之后，窗口内再到的同集其它版本静默跳过，通知里会写明
  「另有 N 条是之前推过的那一集的其它版本，已跳过」。`01v2` 这类修订版不受窗口影响，照推。
  48 小时是覆盖跨天发布、又短于周更番 7 天间隔的折中值；填 `0` 退回旧行为。
  明确**不做**「等 Baha 出了再推」的延迟等待 —— CR 与 Baha 能差一整天，等待窗口要开到
  24 小时以上，等于所有通知统统晚一天。要指定片源请用排除项，那是可控的做法。
- **双语标记分写在两个方括号时仍被单语排除项误杀。** v1.1.6 只认 `简繁日内封`、`CHS&CHT`
  这类**写在一起**的显式标记，而 `[CHS][CHT]`、`[GB][BIG5]`、`[简日][繁日]` 这类分开写的
  照样被「繁体」打死。现在补一条通用判据：标题里同时命中简体侧词**和**繁体侧词就当双语放行，
  并统一把全角 `＆` `＋` 折叠成半角再匹配。

### 变更

- 排除项文案全面改写成**分情况**建议。此前 README 与配置说明一律写「片源别放全局层」，
  这个建议对「订的是宽 feed」成立，但对「订的是某个组的某部番、而这个组稳定同发四个片源」
  是错的 —— 那种场景下把不要的片源填进全局排除项**正是最正当的用法**，因为归并是先到先得，
  不保证留下你要的那一版。现在按订阅类型分两档说明，并保留 `B-Global` 预设含 `Bilibili`
  会连带打死 `[NC-Raws]` 简日双语发布的提醒。
- 管理页排除项面板底部新增「跨轮次窗口」徽标，关掉时直接写明「同集跨天发布会推两次」。
- 数据库 schema 升到 v2，新增 `episode_history` 表记账；删除订阅、按天清理历史都会一起清。

## [1.1.6] - 2026-09-01

### 修复

- **双语单文件被单语排除项误杀，整集悄悄收不到。** 排除是纯子串匹配，而
  `[喵萌][…][简繁日内封]` 里含有「繁日」、`[桜都][…][CHS&CHT]` 里含有 `CHT`，
  所以只要勾了「繁体」，这类**本来就带简体**的双语发布全被丢掉 —— 而且没有任何提示，
  用户只会以为字幕组没出。现在单语排除项遇到双语单文件一律豁免（识别
  `简繁` / `繁简` / `CHS&CHT` / `CHS_CHT` / `GB&BIG5` 等 12 种写法）；
  真想连双语一起躲，勾「简繁」那组预设。
- **短缩写的边界被 strip 掉，误杀正常标题。** `CR` 预设里存的是 `"CR "`（尾部空格就是它的
  词边界），但展开与匹配两侧各 `strip()` 一次，退化成裸 `CR`，于是
  `[Nekomoe kissaten][**Secret** Society]`、`[**Sacred** Blacksmith]` 全部中招。
  现在预设词一律原样保留，只有用户手打的词才去两头空白。
- **落库时也会抹掉词边界。** `store._join` 对每个词 `strip()`，于是即使前两处修好，
  `/sub_exclude apply` 把词表写进订阅后 `"CR "` 又会变回裸 `CR`。同一个坑的第三处。
- **「生肉」和「简体」预设自带误杀风险。** 生肉里的裸 `Raw` 会打死 `[NC-Raws]`、
  `[Lilith-Raws]` 这两个大组的全部发布；简体里的 `GB]` 会命中 `[2.1GB]` 这类体积标注。
  现在改为 `[RAW]` / `(RAW)` / `RAW ` 与 `[GB`，并在词表旁写明「短缩写必须自带边界」。
- **体积标注被当成简体标记，把同集归并的优先顺序也带歪了。** 打标签用的 `RELEASE_TAG_RULES`
  同样收了 `gb]`，于是 `[2.1GB]` 让一条纯繁体发布拿到「简体」标 —— 明明把 `简体` 放在
  `rss_episode_prefer` 第一位，收到的却是繁体版。选源列表上的标记也会跟着标错。
- **单条订阅的黑名单走的是另一套规则。** `Subscription.matches` 里自己写了一句
  `word in title`，上面那些保护只在轮询那条路上生效，per-subscription 黑名单静默误杀。
  现在两条路合用同一个函数。
- 管理页排除项面板的「实际过滤词」预览同样不再 `trim`，跟后端结果对得上了。

### 内部

- 新增 `nexus/excludes.py`：`expand_excludes` / `blocked_by` / `is_dual_language` 三个纯函数
  下沉到独立模块，依赖只有 `constants`。之前它们住在 `services/base.py` 里，
  `models.py` 想复用会绕成循环导入，于是干脆抄了一份 —— 这就是上面最后一个 bug 的成因。
  `services.base` 仍按门面转出这两个名字，调用点无需改动。
- `nexus/constants.py` 新增 `DUAL_LANGUAGE_MARKERS` / `LANGUAGE_ONLY_WORDS`，
  并在 `EXCLUDE_PRESETS` 上方写清「为什么不能和 `RELEASE_TAG_RULES` 合并」。
- 新增 `tests/test_excludes.py`，20 个用例全部用真实字幕组标题做样本，逐条锁死上面的回归。
  测试 515 → 535 个用例。

### 文档

- README 的 16 组预设表按真实词表重写，补上「双语豁免」「短缩写边界」两条规则说明。
- 新增「全局层到底该填什么」一节：`合集` / `生肉` / `720p` 这类「任何形式都不要」的放全局层，
  语言与片源口味交给 `rss_episode_prefer`，并说明宽 feed 是唯一的例外。

## [1.1.5] - 2026-09-01

### 新增

- **真正的全局排除项。** 以前的排除项只有会话一层：换个群就得把「繁体、720p、合集」
  重新勾一遍，装了十个群等于设十遍。现在配置项多了 `global_excludes`，写在这里的词
  对所有会话、所有订阅无条件生效，会话层叠加在它之上继续加严。过滤顺序变成
  **全局层 → 本会话层 → 单条订阅关键词**，任意一层命中就丢。管理页的排除项面板
  会把全局层以只读区块显示出来 —— 不显示的话，用户会把「我没勾却被过滤」当成 bug。
- **同一集的多版本自动归并（`rss_episode_dedup`，默认开启）。** 这才是重复推送的真凶：
  同一个字幕组常把同一集的 Baha / ABEMA、简体 / 繁体、1080p / 720p 各发一条，
  一集刷四五遍。单靠排除项治不了 —— 屏蔽 ABEMA 的代价是 Baha 那天没出片就整集收不到。
  现在同组同集的多个版本会归成一条，按 `rss_episode_prefer` 的口味排序取最优
  （默认「简体 → 1080p → Baha → MKV → 外挂」），落选的版本在通知里以
  「同一集的另外 N 个版本…已自动合并」一行交代清楚，不静默丢东西。
  修订版（v2 / 修正版）永远压过初版。认不出集数的条目（剧场版、合集、公告）原样保留。
- **排除项预设 12 组 → 16 组**：新增 `2160p` / `CR` / `B-Global` / `生肉`，
  顺序也按「语言 → 画质 → 片源 → 字幕形式 → 封装 → 其他」重排，扫一眼就能找到。

### 修复

- **优先级排序权重算错，把「简体」放第一位却收到繁体版。** 旧权重是 `(总数-位次)²`，
  5 项时首位只有 25 分，后面四项 16+9+4+1=30 分能合起来反超。改成二进制权重
  `1 << (总数-位次)`，从数学上保证「命中首位一条 > 后面全部命中之和」。
- **归并对纯字符串标题静默失效。** 默认取标题的回调写成了 `getattr(item, "title", item)`，
  而 `str` 自带一个 `.title()` 方法 —— 喂进裸标题时取到的是绑定方法，分组键全变成
  `built-in method title…`，归并等于没做。
- **同组同周的两部番会互相顶掉（会漏推）。** 归并的分组键早先把标题里所有括号段都抠光，
  `[喵萌奶茶屋]★10月新番★[名探偵プリキュア][22][1080p]` 抠完只剩「10月新番」，
  于是同一个组同一周的不同番剧被当成同一集。现在分组键保留字幕组名与作品名，
  只抠元信息括号段。代价是跨字幕组不归并，这是有意取舍 —— 宁可多推，不可漏推。

### 变更

- 配置项 54 → 57 项（新增 `global_excludes` / `rss_episode_dedup` / `rss_episode_prefer`）。
- 术语统一：会话那层过滤过去在指令与文档里也叫「全局排除项」，现在一律叫
  **「本会话排除项」**，「全局」只指配置项那一层，免得两层混在一起讲不清。

### 内部

- 新增 `nexus/dedup.py`：集数 / 修订号 / 发布标记 / 分组键的解析与归并全是纯函数、无 IO，
  配 28 个用例把上面三个 bug 逐条锁住。
- 新增 `nexus/services/base.py` 的 `global_excludes` / `effective_excludes` / `blocked_by`，
  两层合并只在这一处做，调用点不再各自拼词表。
- 测试 470 → 515 个用例。

## [1.1.4] - 2026-08-30

### 修复

- **`/今日新番` 没有封面，还漏掉了正在播的年番。** 起因是 `/今日新番` 从一开始就是一条
  独立指令，走的是「精简」分支：卡片尺寸和 `/today` 一模一样，却既不取封面、也不查
  长期连载。于是《名探偵プリキュア！》这类年番在这张卡上整条消失，用户看到的是一张
  全是首字占位块的残卡，而且完全无从得知自己踩的是精简模式。省下来的那点渲染时间
  远不值这个误解，所以整条分支删掉：`/今日新番` 现在是 `/today` 的别名，两种叫法
  拿到的是同一张完整卡片。真要少刷屏，把卡片渲染关掉退回纯文本即可。
- **「长期连载」栏最多只列 6 部，多出来的被无声吞掉。** 实测周日在播的年番就有 7 部，
  上限提到 8。
- **卡片写着「共 20 部」却只列出 12 部，中间没有任何交代。** 现在主栏被截断时，
  副标题会明说「今天共 N 部，这里展示前 M 部」。
- **抽番题材找不到时提示会变成一句「可选题材：」后面什么都没有。** 池子走 Bangumi 时
  条目没有题材字段，现在改从条目标签里按出现次数统计。

### 变更

- **抽番数据源默认改为 Bangumi 优先。** 长门番堂带制作组 / 声优 / 题材，但它需要放宽 TLS
  才连得上，不少机房会直接超时 —— 把它放在第一位等于让每次 `/抽番` 都先赌一次网络。
  现在 `gacha_source` 的 `auto` 语义变成「先 Bangumi 每日放送，拿不到再回落长门番堂」，
  想独占长门番堂仍可显式选 `yuc`。默认值本身没变，配置无需改动。
- 指令总数 42 → 41（`今日新番` 由独立指令降级为别名），中文别名 22 → 23。

### 内部

- 新增一组回归测试，把「只有一种今日放送口径」「`compact` 形参不得复活」
  「主栏截断提示进了 HTML」「`_pool` 默认走 Bangumi 且拿到结果就不碰长门番堂」钉死。

## [1.1.3] - 2026-08-30

### 修复

- **`/今日新番` 和每日播报每次都失败。** 1.1.0 给这两张卡加了「放送钟点」和「长期连载」两栏，
  服务层已经按新口径把数据传过去，卡片模板那边却漏改了函数签名，
  于是每次调用都直接抛 `TypeError: build_today_card() got an unexpected keyword argument 'times'`，
  用户只看到一句「这一步没做成」。现在模板补齐了这两个参数：
  钟点显示在评分前面（这张卡的主信息本来就是「今天几点看什么」），
  年番、半年番单独占一栏且不编号 —— 混进主列表会让「今天共 N 部」这个数字失真，
  也会让它们的序号插进当季番的排名里。
- **每日播报卡的副标题一直硬写「按评分从高到低排列」。** 播报的排序是群管自己在配置里选的，
  配成按在看人数或名称排之后，副标题就在骗人。现在副标题跟着实际排序配置走。

### 内部

- 新增签名一致性测试：用 AST 扫出全部 21 个 `build_*_card(...)` 调用点，
  把它们用到的关键字参数跟构造器的真实签名逐一对照。上面那个崩溃之所以能发到线上，
  就是因为原有测试全在测纯函数和数据源，没有一条覆盖「服务层怎么调模板」这条缝。
  今日放送卡的两栏结构也补了单测。

## [1.1.2] - 2026-08-30

### 修复

- **Dashboard 里「删除订阅 / 移除追番 / 清空订阅 / 回写排除词」点了没反应。** AstrBot 的插件页把页面装进
  沙箱 iframe，`sandbox` 只给了 `allow-scripts allow-forms allow-downloads`，**没有 `allow-modals`**。
  按浏览器规范，这种 iframe 里的 `window.confirm()` 不会弹任何东西、并且直接返回「取消」，
  而这四个危险操作原本全靠它守门 —— 于是从用户视角看就是按钮完全失灵。
  现在换成插件自绘的确认框（跟着当前主题配色，支持 Esc 取消 / 回车确认 / 点遮罩取消 / Tab 锁焦点），
  并且是**先问、答应了才开始转圈**，不会出现「弹窗还开着按钮就已经在转」的误导性反馈。

## [1.1.1] - 2026-08-30

### 修复

- **放送时刻和星期能差出快一天。** 放送表的「星期几」按日本当地日期归组（跟 Bangumi 一个口径），
  但显示用的时刻此前按 Bot 所在机器的本地时区算，凌晨还会改写成 `24:xx`。结果《名探偵プリキュア！》这类
  日本时间凌晨 01:00 的番被归进「周日」那一栏，标签却写着 `24:00` —— 按圈内惯例这读作「周六深夜、今晚还没播」，
  实际它当天凌晨就播完了，同一场放送被说成了两个不同的日子。现在展示与归组统一走日本时间，
  不再改写钟点，凌晨档只加「深夜」前缀提示它属于前一晚的档期（如 `深夜 01:30`）。
  `/放送时间` 的那一行同样改为日本时间口径，不会再和「今日新番」互相打脸。

## [1.1.0] - 2026-08-30

这一版的主线是**「订阅一部番」不该等于「被刷屏」**，顺带把几处「明明抓到了数据、卡片上却是空的」补齐。

### 新增

- **订阅先选源。** `/sub 番名` 不再直接收下 Mikan 的关键词搜索源，而是先列出这部番收录了哪些字幕组
  （带最后更新日与简繁 / 画质 / 片源标记），回复序号才落库，订上的是**单组 RSS**（`bangumiId` + `subgroupid`），
  一集只推一条。候选会话只存在内存里、3 分钟过期、同会话重发会顶掉上一次，选完自动撤回那条列表消息。
  新增配置项 `rss_pick_source`（默认开），关掉即恢复旧行为。
- **`/sub_exclude` 全局排除项**（别名 `/排除词`）。按会话维护一份排除词表，标题命中任意一词的发布直接丢弃。
  内置 12 组预设（简体 / 繁体 / 720p / 1080p / 2160p / Baha / ABEMA / CR / B-Global / 合集 / 生肉 / MP4），
  写预设名会自动展开成一串同义写法。新增的订阅自动套用，已有订阅需显式 `apply` 才回写 —— 不偷偷改用户已有配置。
- **`/追番` 之后直接能订。** 加入追番表后会跟一条同样的字幕组候选列表，回序号即完成订阅，
  不用再照着提示手打一遍 `/sub`。
- **新数据源模块 `nexus/sources/mikan.py`。** 解析 Mikan 番组页的字幕组列表（以左栏 `subgroup-{id}` 为准，
  正文里的 `PublishGroup` 编号并不总是同一个）与搜索页的番组 id。
- **查番卡补上制作阵容与主要声优。** 長門番堂拿不到时改走 Bangumi 自己的 `infobox` 与 `/characters` 兜底。
- **卡片外附一段可点击的观看链接**（新增配置项 `show_watch_text`，默认开）。卡片是图片，图里的链接点不动。
- **管理面板订阅页两块新面板。** 「挑一个字幕组」：只填番名点「列字幕组」，从候选里点「订阅这个」；
  「全局排除项」：12 个预设开关 + 自定义词输入 + 展开预览，「保存改动」只存偏好，「回写到已有订阅」才刷已有订阅。
  对应新增 3 条 API 路由（`/subs/sources`、`/excludes` GET/POST）。

### 变更

- **追番回复改为正经卡片。** 原先走的是「纯文本转图」，信息密度和其它卡片对不上。
- 持久层新增 `set_subscription_filters` / `apply_excludes`，把「改用户意图」和「改轮询状态」拆成两个方法，
  避免批量刷新时误伤轮询游标。

### 修复

- **每日播报 100% 失败，只报「没有匹配的平台适配器」。** `Context.send_message` 按平台**实例 id**（通常是 `default`）
  匹配，而用户在配置里几乎一定会填适配器**类型名**（`aiocqhttp`）—— 面板上显眼的就是它。
  现在自动换算成实例 id，并在活动日志里说明纠正了什么；`default_platform_id` 留空则自动识别当前启用的实例。
- **長門番堂（`yuc.wiki`）证书链不全导致整路数据哑掉。** 该站长期只挂半条证书链，部分系统补不齐中间证书会直接握手失败，
  于是季度新番表、制作组、声优全部为空。现在对**硬编码的宽松名单**内主机允许在校验失败时降级重试一次
  （额外配额，不占用原有重试次数），名单外的请求仍严格校验。
- **今日放送卡整卡退化成首字占位块。** bgm 图床的 `l` 档单张就有 1 MB 上下，12 张 base64 内联后 HTML 能涨到十几 MB，
  渲染服务直接超时。现在能改 URL 的改档位、改不了的本地降采样到卡片实际显示尺寸。
- **深夜档番剧在两个星期里各出现一次**，以及**「长期连载」类番剧（如《名探偵プリキュア！》）在今日新番里整条缺失**。
- **AGE 动漫域名轮换。** 官方 README 每 2~3 个月换域名并用删除线标注阵亡域名，上游会把死域名排在前面，
  每次刷新白等五六轮超时。现在解析删除线并剔除，按存活顺序轮换。
- **HTTP 失败被无差别重试。** 现在按超时 / 5xx / 429 / 4xx / DNS 分类，只对值得重试的重试，
  并对确定性失败做短期负缓存，避免同一个挂掉的源每轮都拖满超时。

## [1.0.1] - 2026-08-30

### 变更

- **Logo 重做为樱粉配色。** 五瓣樱花对应五路数据源向中枢汇聚，花心的播放三角是中枢本体，
  外圈高亮弧与端点游标表示追番进度。配色取自 `sakura` 主题的强调色，和卡片、管理面板是同一套色谱。
  同时 `logo.svg` 不再手工维护两份副本，改由构建脚本从 `nexus/render/logo.py` 统一导出，
  彻底消除 README 与 WebUI 里 logo 不一致的可能。

### 修复

- **构建脚本在内存吃紧的机器上会崩。** 原先固定用 2 倍设备像素比截全页图，
  Chromium 需要一次性合成 3120×4300 的位图，Pillow 缩放时还要再吃几百 MB 浮点缓冲，
  内存不足时会抛 `Unable to capture screenshot` 或 `MemoryError`，整个构建直接失败。
  现在默认降到 1.5 倍（成图仍统一缩到 1950 像素宽，肉眼无差别）、缩放走 `reducing_gap` 分两步、
  每张卡片用独立浏览器上下文并即时释放；万一还是失败会沿 2.0 → 1.5 → 1.25 → 1.0 自动降档重试，
  并且降档后不再回升，保证六张卡清晰度一致。新增 `--scale` 参数供内存充裕的机器手动拉高。

## [1.0.0] - 2026-08-30

首个发布版本。番剧中枢把原先需要多个插件才能拼出来的番剧链路（查番 → 追番 → 订阅 → 播报）
合并成一个插件，并补上了跨源匹配、卡片渲染兜底、人格转述与管理面板。

### 新增

- **跨源聚合**：以 bangumi-data 的跨站 ID 表为枢纽，把 Bangumi 条目、長門番堂的制作组／声优、
  anime1 在线索引、AGE 动漫更新集数、萌娘百科词条、Mikan RSS 地址匹配到同一部番。
- **41 条指令 / 6 个分类 / 21 个中文别名**，指令目录集中在 `nexus/catalog.py`，帮助卡与管理页共用同一份数据。
- **4 个函数工具（LLM tool）**：`get_anime_list`、`get_watch_url`、`search_moegirl`、`search_bangumi_subject`，
  让人格可以自己去查番。
- **6 套卡片主题 × 13 套 HTML 版式**：午夜霓虹 / 极光 / 樱绯 / 蓝图 / 素笺 / 落日，
  主题变量集中在 `nexus/render/themes.py`，卡片与管理页共用。
- **4 级渲染降级链**：AstrBot 无头浏览器 → Pillow 本地绘制 → t2i 文字转图 → 纯文本。
  HTML 渲染连续失败 3 次进入 120 秒冷却；渲染结果按内容哈希缓存（最多 240 个文件 / 保留 6 小时）。
- **人格转述**：新番播报、RSS 更新、Webhook 事件会先让当前 AstrBot 人格用自己的口吻说一句，再带卡片一起发。
  可指定人格、指定 LLM 供应商、限制字数。
- **RSS 订阅**：支持 Mikan 单番 / Mikan 搜索 / RSSHub 路由 / 蜜柑经典源 / 动漫花园 / 任意 RSS 地址六种写法，
  只给番名也能自动找到 Mikan 源。带去重、指数退避、并发闸门、首轮静默入库。
- **AutoBangumi Webhook 接入**：双通道（Dashboard `/api/plug/…` 与可选独立监听端口），
  支持校验令牌、事件分类、精确路由到追这部番的会话、新集入库自动推进追番进度。
- **每日播报**：按会话订阅、按星期手动触发、评分与在看人数门限、排序方式可配。
- **追番管理**：进度 / 状态 / 评分 / 备注 / 总集数，四种状态（在追 / 想看 / 已看完 / 已弃坑）。
- **Dashboard 管理面板**：9 个视图（概览 / 配置 / 追番 / 订阅 / 播报 / 卡片 / 数据源 / 指令 / 关于）、
  6 套主题、紧凑模式、移动端自适应，29 条后端路由，界面偏好存后端。
- **数据源体检**：8 个数据源逐个探测，给出耗时与失败原因（`/番剧诊断`）。
- **导入导出**：订阅与追番表可导出为 JSON、可粘贴导入。
- **每会话独立设置**：卡片主题、渲染方式、结果版式、是否收播报、播报投递目标。
- **环形活动日志**：最近 240 条运行记录，可按级别过滤，管理页直接看。
- **抽番与推荐**：`/抽番 [题材]` 走長門番堂季度表，`/番剧推荐` 走 AGE 动漫推荐位。

### 相对上游插件的修复与改进

- **标题匹配**：补上半角化、罗马音、季数别名归一化，中日繁三种写法都能对上；
  上游按原始字符串比对，中日混排时经常匹配不到。
- **RSS 首轮不再刷屏**：新订阅第一次轮询只入库不推送，上游会把整个历史条目一次性推出来。
- **RSS 失败只报一次**：同一个源连续失败时改为指数退避并只提示一次，上游每轮都报错。
- **HTTP 层统一**：重试只对超时 / 5xx / 429 生效，带并发闸门与内存缓存；
  上游各自 `requests.get` 无重试无缓存，一个站慢会拖住整个流程。
- **调度对齐整分钟**：自写调度循环对齐到整分钟边界，不再出现「大约 08:30」的漂移。
- **播报目标校验**：目标会话标识写错时明确报错并列出可用会话，上游静默失败。
- **卡片渲染不再是单点**：上游渲染失败即功能不可用，这里逐级降级到纯文本。
- **配置热生效**：网络类改动热重建 HTTP 客户端，定时类改动重排调度，不需要重载插件。
- **限额与清理**：单会话订阅 60 条 / 追番 300 条上限，去重历史按天数清理，渲染缓存按数量与时效清理。
- **刷新时段解析**：带符号写法（如 `-1`）不再被抽成 `1` 静默生效，一律判为无效并跳过。
- **封面相对路径补全**：`/x.jpg` 这类相对地址不再被拼成 `https:/x.jpg`，封面不会整片挂掉。
- **首播时间不再误取「官网」**：详情表取不到时间时直接留空，不再兜底截取单元格文本。
- **日历有、详情表没有的番会被补齐**：以日历表为准，至少保留番名与封面。
- **单测**：280 个用例，全部离线（fixture 化的 HTML 样本），覆盖标题归一化、季度换算、RSS / Webhook 解析、
  配置强转、SQLite 持久层、管理页 API，以及帮助卡指令与实际注册的一致性。

### 已知取舍

- 未迁移上游 `astrbot_plugin_rsshub` 的知识库问答（`rsshub_kb_*`）相关指令，与番剧主线无关。
- `feedparser` 为可选依赖，缺失时走标准库解析，覆盖面略窄但可用。
- 長門番堂（yuc.wiki）对未开播季度可能返回 404，此时自动回落 Bangumi。

[1.0.1]: https://github.com/Whereis-Alice/astrbot_plugin_bangumi_nexus/releases/tag/v1.0.1
[1.0.0]: https://github.com/Whereis-Alice/astrbot_plugin_bangumi_nexus/releases/tag/v1.0.0