# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.1] - 2026-08-30

### 修复

- **放送时刻和星期能差出快一天。** 放送表的「星期几」按日本当地日期归组（跟 Bangumi 一个口径），
  但显示用的时刻此前按 Bot 所在机器的本地时区算，凌晨还会改写成 `24:xx`。结果《名探偵プリキュア！》这类
  日本时间凌晨 01:00 的番被归进「周日」那一栏，标签却写着 `24:00` —— 按圈内惯例这读作「周六深夜、今晚还没播」，
  实际它当天凌晨就播完了，同一场放送被说成了两个不同的日子。现在展示与归组统一走日本时间，
  不再改写钟点，凌晨档只加「深夜」前缀提示它属于前一晚的档期（如 `深夜 01:30`）。
  `/放送时间` 的那一行同样改为日本时间口径，不会再和「今日新番」互相打脸。

## [1.1.0] - 2026-08-30

这一版的主线是**「订阅一部番」不该等于「被刷屏」**，顺带把几处「明明抓到了数据、卡片上却是空的」补齐。

### 新增

- **订阅先选源。** `/sub 番名` 不再直接收下 Mikan 的关键词搜索源，而是先列出这部番收录了哪些字幕组
  （带最后更新日与简繁 / 画质 / 片源标记），回复序号才落库，订上的是**单组 RSS**（`bangumiId` + `subgroupid`），
  一集只推一条。候选会话只存在内存里、3 分钟过期、同会话重发会顶掉上一次，选完自动撤回那条列表消息。
  新增配置项 `rss_pick_source`（默认开），关掉即恢复旧行为。
- **`/sub_exclude` 全局排除项**（别名 `/排除词`）。按会话维护一份排除词表，标题命中任意一词的发布直接丢弃。
  内置 12 组预设（简体 / 繁体 / 720p / 1080p / 2160p / Baha / ABEMA / CR / B-Global / 合集 / 生肉 / MP4），
  写预设名会自动展开成一串同义写法。新增的订阅自动套用，已有订阅需显式 `apply` 才回写 —— 不偷偷改用户已有配置。
- **`/追番` 之后直接能订。** 加入追番表后会跟一条同样的字幕组候选列表，回序号即完成订阅，
  不用再照着提示手打一遍 `/sub`。
- **新数据源模块 `nexus/sources/mikan.py`。** 解析 Mikan 番组页的字幕组列表（以左栏 `subgroup-{id}` 为准，
  正文里的 `PublishGroup` 编号并不总是同一个）与搜索页的番组 id。
- **查番卡补上制作阵容与主要声优。** 長門番堂拿不到时改走 Bangumi 自己的 `infobox` 与 `/characters` 兜底。
- **卡片外附一段可点击的观看链接**（新增配置项 `show_watch_text`，默认开）。卡片是图片，图里的链接点不动。
- **管理面板订阅页两块新面板。** 「挑一个字幕组」：只填番名点「列字幕组」，从候选里点「订阅这个」；
  「全局排除项」：12 个预设开关 + 自定义词输入 + 展开预览，「保存改动」只存偏好，「回写到已有订阅」才刷已有订阅。
  对应新增 3 条 API 路由（`/subs/sources`、`/excludes` GET/POST）。

### 变更

- **追番回复改为正经卡片。** 原先走的是「纯文本转图」，信息密度和其它卡片对不上。
- 持久层新增 `set_subscription_filters` / `apply_excludes`，把「改用户意图」和「改轮询状态」拆成两个方法，
  避免批量刷新时误伤轮询游标。

### 修复

- **每日播报 100% 失败，只报「没有匹配的平台适配器」。** `Context.send_message` 按平台**实例 id**（通常是 `default`）
  匹配，而用户在配置里几乎一定会填适配器**类型名**（`aiocqhttp`）—— 面板上显眼的就是它。
  现在自动换算成实例 id，并在活动日志里说明纠正了什么；`default_platform_id` 留空则自动识别当前启用的实例。
- **長門番堂（`yuc.wiki`）证书链不全导致整路数据哑掉。** 该站长期只挂半条证书链，部分系统补不齐中间证书会直接握手失败，
  于是季度新番表、制作组、声优全部为空。现在对**硬编码的宽松名单**内主机允许在校验失败时降级重试一次
  （额外配额，不占用原有重试次数），名单外的请求仍严格校验。
- **今日放送卡整卡退化成首字占位块。** bgm 图床的 `l` 档单张就有 1 MB 上下，12 张 base64 内联后 HTML 能涨到十几 MB，
  渲染服务直接超时。现在能改 URL 的改档位、改不了的本地降采样到卡片实际显示尺寸。
- **深夜档番剧在两个星期里各出现一次**，以及**「长期连载」类番剧（如《名探偵プリキュア！》）在今日新番里整条缺失**。
- **AGE 动漫域名轮换。** 官方 README 每 2~3 个月换域名并用删除线标注阵亡域名，上游会把死域名排在前面，
  每次刷新白等五六轮超时。现在解析删除线并剔除，按存活顺序轮换。
- **HTTP 失败被无差别重试。** 现在按超时 / 5xx / 429 / 4xx / DNS 分类，只对值得重试的重试，
  并对确定性失败做短期负缓存，避免同一个挂掉的源每轮都拖满超时。

## [1.0.1] - 2026-08-30

### 变更

- **Logo 重做为樱粉配色。** 五瓣樱花对应五路数据源向中枢汇聚，花心的播放三角是中枢本体，
  外圈高亮弧与端点游标表示追番进度。配色取自 `sakura` 主题的强调色，和卡片、管理面板是同一套色谱。
  同时 `logo.svg` 不再手工维护两份副本，改由构建脚本从 `nexus/render/logo.py` 统一导出，
  彻底消除 README 与 WebUI 里 logo 不一致的可能。

### 修复

- **构建脚本在内存吃紧的机器上会崩。** 原先固定用 2 倍设备像素比截全页图，
  Chromium 需要一次性合成 3120×4300 的位图，Pillow 缩放时还要再吃几百 MB 浮点缓冲，
  内存不足时会抛 `Unable to capture screenshot` 或 `MemoryError`，整个构建直接失败。
  现在默认降到 1.5 倍（成图仍统一缩到 1950 像素宽，肉眼无差别）、缩放走 `reducing_gap` 分两步、
  每张卡片用独立浏览器上下文并即时释放；万一还是失败会沿 2.0 → 1.5 → 1.25 → 1.0 自动降档重试，
  并且降档后不再回升，保证六张卡清晰度一致。新增 `--scale` 参数供内存充裕的机器手动拉高。

## [1.0.0] - 2026-08-30

首个发布版本。番剧中枢把原先需要多个插件才能拼出来的番剧链路（查番 → 追番 → 订阅 → 播报）
合并成一个插件，并补上了跨源匹配、卡片渲染兜底、人格转述与管理面板。

### 新增

- **跨源聚合**：以 bangumi-data 的跨站 ID 表为枢纽，把 Bangumi 条目、長門番堂的制作组／声优、
  anime1 在线索引、AGE 动漫更新集数、萌娘百科词条、Mikan RSS 地址匹配到同一部番。
- **41 条指令 / 6 个分类 / 21 个中文别名**，指令目录集中在 `nexus/catalog.py`，帮助卡与管理页共用同一份数据。
- **4 个函数工具（LLM tool）**：`get_anime_list`、`get_watch_url`、`search_moegirl`、`search_bangumi_subject`，
  让人格可以自己去查番。
- **6 套卡片主题 × 13 套 HTML 版式**：午夜霓虹 / 极光 / 樱绯 / 蓝图 / 素笺 / 落日，
  主题变量集中在 `nexus/render/themes.py`，卡片与管理页共用。
- **4 级渲染降级链**：AstrBot 无头浏览器 → Pillow 本地绘制 → t2i 文字转图 → 纯文本。
  HTML 渲染连续失败 3 次进入 120 秒冷却；渲染结果按内容哈希缓存（最多 240 个文件 / 保留 6 小时）。
- **人格转述**：新番播报、RSS 更新、Webhook 事件会先让当前 AstrBot 人格用自己的口吻说一句，再带卡片一起发。
  可指定人格、指定 LLM 供应商、限制字数。
- **RSS 订阅**：支持 Mikan 单番 / Mikan 搜索 / RSSHub 路由 / 蜜柑经典源 / 动漫花园 / 任意 RSS 地址六种写法，
  只给番名也能自动找到 Mikan 源。带去重、指数退避、并发闸门、首轮静默入库。
- **AutoBangumi Webhook 接入**：双通道（Dashboard `/api/plug/…` 与可选独立监听端口），
  支持校验令牌、事件分类、精确路由到追这部番的会话、新集入库自动推进追番进度。
- **每日播报**：按会话订阅、按星期手动触发、评分与在看人数门限、排序方式可配。
- **追番管理**：进度 / 状态 / 评分 / 备注 / 总集数，四种状态（在追 / 想看 / 已看完 / 已弃坑）。
- **Dashboard 管理面板**：9 个视图（概览 / 配置 / 追番 / 订阅 / 播报 / 卡片 / 数据源 / 指令 / 关于）、
  6 套主题、紧凑模式、移动端自适应，29 条后端路由，界面偏好存后端。
- **数据源体检**：8 个数据源逐个探测，给出耗时与失败原因（`/番剧诊断`）。
- **导入导出**：订阅与追番表可导出为 JSON、可粘贴导入。
- **每会话独立设置**：卡片主题、渲染方式、结果版式、是否收播报、播报投递目标。
- **环形活动日志**：最近 240 条运行记录，可按级别过滤，管理页直接看。
- **抽番与推荐**：`/抽番 [题材]` 走長門番堂季度表，`/番剧推荐` 走 AGE 动漫推荐位。

### 相对上游插件的修复与改进

- **标题匹配**：补上半角化、罗马音、季数别名归一化，中日繁三种写法都能对上；
  上游按原始字符串比对，中日混排时经常匹配不到。
- **RSS 首轮不再刷屏**：新订阅第一次轮询只入库不推送，上游会把整个历史条目一次性推出来。
- **RSS 失败只报一次**：同一个源连续失败时改为指数退避并只提示一次，上游每轮都报错。
- **HTTP 层统一**：重试只对超时 / 5xx / 429 生效，带并发闸门与内存缓存；
  上游各自 `requests.get` 无重试无缓存，一个站慢会拖住整个流程。
- **调度对齐整分钟**：自写调度循环对齐到整分钟边界，不再出现「大约 08:30」的漂移。
- **播报目标校验**：目标会话标识写错时明确报错并列出可用会话，上游静默失败。
- **卡片渲染不再是单点**：上游渲染失败即功能不可用，这里逐级降级到纯文本。
- **配置热生效**：网络类改动热重建 HTTP 客户端，定时类改动重排调度，不需要重载插件。
- **限额与清理**：单会话订阅 60 条 / 追番 300 条上限，去重历史按天数清理，渲染缓存按数量与时效清理。
- **刷新时段解析**：带符号写法（如 `-1`）不再被抽成 `1` 静默生效，一律判为无效并跳过。
- **封面相对路径补全**：`/x.jpg` 这类相对地址不再被拼成 `https:/x.jpg`，封面不会整片挂掉。
- **首播时间不再误取「官网」**：详情表取不到时间时直接留空，不再兜底截取单元格文本。
- **日历有、详情表没有的番会被补齐**：以日历表为准，至少保留番名与封面。
- **单测**：280 个用例，全部离线（fixture 化的 HTML 样本），覆盖标题归一化、季度换算、RSS / Webhook 解析、
  配置强转、SQLite 持久层、管理页 API，以及帮助卡指令与实际注册的一致性。

### 已知取舍

- 未迁移上游 `astrbot_plugin_rsshub` 的知识库问答（`rsshub_kb_*`）相关指令，与番剧主线无关。
- `feedparser` 为可选依赖，缺失时走标准库解析，覆盖面略窄但可用。
- 長門番堂（yuc.wiki）对未开播季度可能返回 404，此时自动回落 Bangumi。

[1.0.1]: https://github.com/Whereis-Alice/astrbot_plugin_bangumi_nexus/releases/tag/v1.0.1
[1.0.0]: https://github.com/Whereis-Alice/astrbot_plugin_bangumi_nexus/releases/tag/v1.0.0